const config = {
    db: {
        host: "localhost",
        user: "root",
        password: "password",
        database: "database"
    },
    email: {
        mail: "mail",
        password: "password",
        from: "from"
    },
    secret: {
        secretkey: "secretkey"
    }
}

module.exports = config;